# 처음 오신 분께

부자주방 소싱 분석기 개발 환경 만드는 법입니다. 30분이면 됩니다.

## 1. 준비물

- 맥 또는 윈도우 컴퓨터
- GitHub 계정 (사장님께 저장소 초대를 받으세요)
- Claude 계정 (Claude Code 설치용)

## 2. 설치

**① Python 3.12**
- 맥: 터미널에 `python3 --version` — 3.10 이상이면 그대로 씁니다
- 윈도우: python.org 에서 받아 설치 (설치할 때 "Add to PATH" 체크)

**② 코드 받기**
```bash
git clone https://github.com/bujajubang-ship-it/bujajubang-analyzer.git
cd bujajubang-analyzer
pip install -r requirements.txt
```

**③ Claude Code 설치**
- https://claude.com/code 안내를 따라 설치하고 본인 계정으로 로그인
- 터미널에서 `cd bujajubang-analyzer` 후 `claude` 실행
- 이 폴더의 `CLAUDE.md`를 Claude가 자동으로 읽고 규칙을 지킵니다

## 3. 띄워보기

```bash
DATA_DIR=. python3 -m uvicorn main:app --reload --port 8300
```
브라우저에서 `http://localhost:8300` → **로그인 화면이 뜨면 정상입니다.**
계정은 사장님께 받으세요.

일부 탭은 로컬에서 "데이터가 없습니다"라고 뜹니다. **정상입니다** —
실제 데이터(54MB)는 서버에만 있고 코드에는 안 들어 있습니다.

## 4. 작업하는 법

**main 브랜치에 직접 올리지 마세요.** 올리는 순간 실서비스가 바뀝니다.

```bash
git switch -c fix/무엇을-고치는지     # 새 가지를 만들고
# ... 작업 ...
git add -A && git commit -m "무엇을 왜 고쳤는지"
git push -u origin fix/무엇을-고치는지  # 올린 뒤 사장님께 알립니다
```

사장님이 확인하고 합치면 그때 실서비스에 나갑니다.

## 5. 하면 안 되는 것

- ❌ **API 키를 코드에 쓰기** — 키는 서버에만 있습니다. 필요하면 사장님께 문의하세요.
- ❌ **`.env` 파일을 git에 올리기** — 이미 막아뒀지만 강제로 올리지 마세요.
- ❌ **로그인 잠금 풀기** — 회사 매출·거래처 자료가 들어 있습니다.
- ❌ **main에 직접 push**
- ❌ **라이트세일 서버 작업** — 쿠팡 재고 자동화가 돌고 있습니다. 멈추면 상품이 품절로 바뀝니다.

## 6. 막히면

Claude Code에게 그냥 한국어로 물어보세요. 이 프로젝트 규칙(`CLAUDE.md`)을 이미 알고 있습니다.

> "시장조사 탭에 000 기능을 넣고 싶어"
> "이 에러가 나는데 왜 그래?"

그래도 안 되면 사장님께 물어보세요.
